# Canvas workspace redesign and Chrome verification

Date: 2026-09-14. Base: `d58a8f7`. Branch: `codex/kras-investigation`.

## Product review

The former layout gave the molecular view, findings, evidence and pair analysis similar visual weight. This obscured the primary task: inspect a structural context, select a residue or variant, connect it to measurements, and record a testable interpretation. Long forms and calculation details competed with the structure before the researcher needed them.

The redesign makes the molecular canvas the main workspace. A compact rail opens one contextual panel; the assay explorer expands beneath the structure. Measured outcomes come first in selection details. Comparison methods and sources have defined homes. Findings and experiments share a notebook with compact saved entries and explicit editing. A linked sequence, direct variant/residue search, region shortcuts, overlay/split views, and focus/reset controls connect these surfaces.

This is a UI and workflow change. The real KRAS case, existing scientific calculations, provenance, durable revision handling, recovery, imports and exports remain in use. Both experimental structures carry G12V in different binding contexts; selecting another substitution does not alter their coordinates or establish a causal mechanism.

## Implementation and fixes

- Replaced the crowded page with a large dark molecular stage, light contextual panels, restrained amber/cyan structural encoding, compact sequence and collapsible assays.
- Extracted the notebook, sequence, dialog, icons and contextual panels while retaining the existing session/job backend.
- Added validated reference-residue and substitution search, including combination variants. Residue details offer measured single substitutions.
- Kept assay filters and notebook entry drafts mounted when switching views. Fixed an active residue filter becoming impossible to clear after selection was cleared.
- Added drawer focus entry/restoration, including a stable fallback when the bottom launcher is removed. Narrow panels use an inert background and Escape handling.
- Fixed Molstar reuse of a disposed WebGL canvas during effect restarts. Each plugin owns a fresh canvas; resize observation is disposed with it.
- Reset split/overlay framing on mode changes and stopped replaying stale focus commands after viewer recreation.
- Fixed camera reset after a viewport change: recompute fitting distance at the current dimensions while preserving canonical target, direction and up vector.
- Centered native dialogs; made notebook controls fit the sidebar; clarified reference-validation wording and copy titles.

## Observed Chrome checks

Testing used the actual local app in Google Chrome through native accessibility and pointer controls. Chrome's device toolbar was used for a **390 × 844** viewport, and the browser zoom indicator confirmed **200%**. The editable tests used a separate investigation named **QA · canvas workflow**, ID `20ee7c12-97fa-431e-bd1f-91bb75d8f4a6`. Existing user findings were not overwritten or deleted.

| Area | Observed result |
| --- | --- |
| Investigation settings | Created a copy and renamed it; dialogs and save status worked. |
| Search | Invalid Q12 reports the reference-residue mismatch without replacing the selection; lowercase G12D selects the measured variant. |
| Sequence | Residue selection, arrow navigation and the nine-residue Switch I shortcut update the linked context. |
| Molecular selection | Clicking the 3D structure selected K42 and updated the visible selection chip and sequence. |
| Overlay, split and focus mode | Both split structures fit completely after the framing fix. Expanded overlay renders a large molecular model. Escape exits expanded mode with Chrome focused. |
| Comparison | A real outside-switch fit completed with 138 matched atoms and RMSD 0.438 Å. The fit choice and result were restored. |
| Assay filter | A selected-residue filter can still be unchecked after clearing selection; all variants become available again. |
| Assay table | G12D search returned 833 matching rows, exact G12D first. Selecting its row updated the molecular selection. Missing measurements remain explicitly labeled. |
| Drawer keyboard | Opening from the bottom launcher focuses Close assays. Keyboard activation of Close assays restores focus to the stable Assays rail control. |
| Finding workflow | Created a hypothesis with a claim, contradictory explanation, G12D and residue 12. An unfinished title edit survived Findings → Experiments → Findings and then saved. |
| Experiment workflow | Zero repeats is rejected; three repeats is accepted. Duplicate G12D is rejected. Saved G12D as a candidate and WT as a positive control, with rationale and expected observations. |
| Brief and exports | Brief showed the saved claim and contradiction. Invoked all four export controls; download availability appeared in Chrome. Export payload integrity was additionally verified against the production API. |
| Zoomed layout | At 200%, the notebook became an overlay with readable entries and visible close/navigation controls. |
| Phone layout | At 390 × 844, the rail, search, compact sequence, toolbar and notebook remained usable. The camera-reset fix visibly fitted the full structure. |
| Console during development | The inspected error was an installed browser extension's unload-permission warning. Two development-only warnings came from Molstar's mutative dependency. No application exception was identified in that inspected console output. |

Keyboard controls were tested with the Chrome window explicitly raised when necessary. A complete keyboard-only audit, an automated accessibility scan, free-rotation verification, and plot/displacement point-click coverage were not completed. Those are not implied by the checks above.

## Automated and production verification

- Full application suite: **57 passed, 0 failed**.
- Type checking and lint: passed.
- Production build: passed.
- Patch whitespace check: passed.
- The new responsive camera regression failed before implementation and passed afterward. Its final version changes both camera position and up vector, verifies current-viewport fitting, and checks canonical orientation. All six camera tests passed again after the final orientation correction.
- Existing `scripts/verify-api.mjs` passed against the production server: real case loading, saved revisions, conflicting saves, durable comparison execution, job deduplication, independent editable copies, four export formats, and reproducible bundle import.
- A separate read-only production check confirmed that the **Chrome-authored** QA finding and two experiments survived the server restart at revision 26. It asserted the contradiction, G12D attachment, experiment roles/repeats, export attachment headers, saved JSON content, Markdown claim/control content, CSV rows and MVSX ZIP signature. Export sizes were JSON 24,193,544 bytes; Markdown 3,528; CSV 381; MVSX 659,006.
- Independent code review covered the implementation and follow-up fixes. Three P2 findings (filter reset, retained notebook edits, drawer focus) and the final camera orientation issue were resolved. The scoped review closed with no outstanding findings.

## Handoff

The final production-browser pass resumed after the user unlocked the Mac. The production page was reloaded in Chrome and the scrolling and dialog checks passed. The final warning/error log was empty. The desktop viewport was restored to 1291 × 806; Chrome is left on **KRAS · binding and abundance**, with its camera fitted to the canvas and all changes saved. The production preview is served at `http://127.0.0.1:4311/`.

QA and verification investigations are retained under their explicit names. No deployment, merge, push, training run, or modification of the parent project's synced sources was performed.

## Follow-up: scrolling and basic usability

After the user unlocked the Mac, testing resumed in the dedicated Chrome review tab. Chrome tab changes initially interrupted native control; the user then explicitly left Chrome idle. Direct tab controls became available and were used for pointer/keyboard interaction and read-only DOM geometry measurements.

The reported scrolling problem was reproduced at 1150 × 650: the assay drawer ended at y=749.5 while the viewport ended at y=650, and the chart started at y=653.4, entirely below the visible area. Opening the drawer focused its Close button with scrolling suppressed. The workspace, drawer and chart also formed nested scroll containers, while the protein itself consumed wheel input for zoom.

The follow-up makes drawer opening bring its full bounds into view, removes the redundant outer drawer scroller, allows chart scrolling to pass to the workspace at its boundary, and wraps the assay toolbar according to its actual container width. The checkbox label now stays on one line. Long dialogs now have a fixed header and a separate scrolling body. Dialogs explicitly wrap keyboard focus and retain their opener for return focus; narrow panels exclude controls inside closed disclosure sections from their focus loop.

Observed regression checks:

- At 1150 × 650, the workspace scrolls to 157 on opening; the drawer ends at y=650 and chart content at y=638. Normal wheel scrolling reaches the chart disclosure at y=637.97.
- The variant table scrolls through all 40 rows to its 1160.5px bottom; Next advances from page 1 to page 2, with pagination visible.
- At 390 × 700, the drawer ends at y=700 and the displacement caption is reachable at y=688.14. Keyboard activation selects Q61; pointer activation selects E62, updating both the bar and linked selection.
- The details panel scrolls to its final next-step action. A long new-finding form scrolls to the bottom of its 425px overflow.
- Before the dialog fix, scrolling moved its Close button to y=-363.25. After the fix, Close stays at y=61.25 while the body scrolls 432.5px and the export controls remain reachable.
- Shift+Tab from the narrow notebook's Close button lands on its visible saved-entry summary. Escape closes the overlay and returns focus to the Notebook tool.
- Dialog Shift+Tab wraps from Close to the final MVSX export, and Tab wraps back to Close.
- In the final production build, Escape from the brief returns focus to Review brief. Escape from settings returns focus to its original settings launcher. Opening assays at 1150 × 650 again scrolls the workspace to 157 and brings the drawer bottom to y=650.

The follow-up full application suite passed again: 57 tests, no failures; type checking and lint also passed. The final production build, including explicit dialog-opener restoration, passed. Independent source review accepted the scroll-container, wrapping, dialog-header and focus-loop changes. Molecular wheel input still intentionally zooms the structure; the surrounding content scrolls within its own panels.
